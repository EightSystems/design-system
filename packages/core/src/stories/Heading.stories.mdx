import { Meta, Story, Canvas, Description, ArgsTable } from "@storybook/addon-docs/blocks";
import { Heading } from "../components/typography/Heading";

<Meta title="Typography/Heading" component={Heading} />

# Heading

The `Heading` component is part of the typography module, you can use it as a substitute to `H1` - `H6` HTML heading tags. It will also simulate the same tags in Native

## Import

```jsx
import { Heading } from "@8sistemas/design-system";
```

## Usage

<Canvas>
    <Story name="H1" args={{elementLevel: 'h1', children: ['Sample H1']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="H2" args={{elementLevel: 'h2', children: ['Sample H2']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="H3" args={{elementLevel: 'h3', children: ['Sample H3']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="H4" args={{elementLevel: 'h4', children: ['Sample H4']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="H5" args={{elementLevel: 'h5', children: ['Sample H5']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="H6" args={{elementLevel: 'h6', children: ['Sample H6']}}>
        {args => <Heading {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Heading} />
