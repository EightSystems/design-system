import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Stepper from "../components/Stepper";

<Meta title="Molecules/Stepper" component={Stepper} />

# Stepper

The `Stepper` shows progress trough a sequence of linear logical steps.

<Canvas>
    <Story
        name="Stepper"
        args={{
            activeStepLabel: "Meio de pagamento",
            nextStepLabel: "Revisão",
            currentStepPercentage: 50,
            currentStep: 2,
            maxSteps: 4,
        }}
    >
        {args => <Stepper {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Stepper} />
