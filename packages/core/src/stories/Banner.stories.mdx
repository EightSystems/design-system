import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Banner } from "../components/feedback/Banner";
import { action } from "@storybook/addon-actions";

<Meta title="Feedback/Banner" component={Banner} />

# Banner

A banner displays a prominent message.

## Import

```jsx
import { Banner } from "@8sistemas/design-system";
```

## Example

### Warning

<Canvas>
    <Story name="Warning">
        {args => {
            return <Banner status={"warning"} onClose={action("onClose")} {...args} />;
        }}
    </Story>
</Canvas>

### Info

<Canvas>
    <Story name="Info">
        {args => {
            return <Banner status={"info"} onClose={action("onClose")} {...args} />;
        }}
    </Story>
</Canvas>

### Custom Background

<Canvas>
    <Story name="Custom Background">
        {args => {
            return <Banner status={"success"} backgroundColor={"primary"} description={'Description test'} onClose={action("onClose")} {...args} />;
        }}
    </Story>
</Canvas>

## Props

<ArgsTable of={Banner} />
