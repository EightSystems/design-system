import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Switch } from "../components/forms/Switch";
import { action } from "@storybook/addon-actions";

<Meta title="Forms/Switch" component={Switch} />

# Switch

Switch is a visual toggle between two mutually exclusive states — on and off.

## Import

```jsx
import { Switch } from "@8sistemas/design-system";
```

## Examples

### Unchecked

<Canvas>
    <Story name="Unchecked">
        {args => {
            return <Switch {...args} label={"Unchecked"} onValueChange={action("Unchecked")} />;
        }}
    </Story>
</Canvas>

### Checked

<Canvas>
    <Story name="Checked">
        {args => {
            return <Switch {...args} checked={true} label={"Checked"} onValueChange={action("Checked")} />;
        }}
    </Story>
</Canvas>

## Props

<ArgsTable of={Switch} />
