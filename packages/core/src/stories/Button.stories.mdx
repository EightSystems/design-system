import { Meta, Story, Canvas, Description, ArgsTable } from "@storybook/addon-docs/blocks";
import { Button } from "../components/forms/Button";

<Meta title="Atoms/Button" component={Button} />

# Button

The `Button` component is used to trigger an action or event, such as submitting a form, opening a dialog, canceling an
action, or performing a delete operation.

## Import

```jsx
import { Button } from "@8sistemas/design-system";
```

## Usage

<Canvas>
    <Story name="Primary Button" args={{ backgroundColor: "primary", children: ["Primary Button"] }}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

## Button Variants

You can use the various `borderColor`, `backgroundColor`, and `textColor` for creating variants on your self. For
example, see an outline button sample

<Canvas>
    <Story
        name="Primary Button (Outline)"
        args={{
            backgroundColor: "lightTint",
            textColor: "primary",
            borderColor: "primary",
            children: ["Outlined Button"],
        }}
    >
        {args => <Button {...args} />}
    </Story>
</Canvas>

Use the `backgroundColor` prop to determinate the color of the button using the `Theme` available primary colors.

<Canvas>
    <Story name="Danger Button" args={{ backgroundColor: "danger", children: ["Danger Button"] }}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

## Gradient Support

We support basic gradient background, you can use the `backgroundColor` prop with one of the `*Gradient` values. For
example, `primaryGradient`. Be aware that it's a simple linear-gradient.

### Danger button with Gradient

<Canvas>
    <Story
        name="Primary Button with Gradient"
        args={{ backgroundColor: "primaryGradient", children: ["Primary Button with Gradient"] }}
    >
        {args => <Button {...args} />}
    </Story>
</Canvas>

### Danger button with Gradient

<Canvas>
    <Story
        name="Danger Button with Gradient"
        args={{ backgroundColor: "dangerGradient", children: ["Danger Button with Gradient"] }}
    >
        {args => <Button {...args} />}
    </Story>
</Canvas>

### Success button sample

<Canvas>
    <Story name="Success Button" args={{ backgroundColor: "success", children: ["Success Button"] }}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

### Font Size large

<Canvas>
    <Story
        name="Success Button with a large font"
        args={{ backgroundColor: "success", children: ["Large Font"], fontSize: "lg" }}
    >
        {args => <Button {...args} />}
    </Story>
</Canvas>

Secondary Button Sample

<Canvas>
    <Story name="Secondary Button" args={{ backgroundColor: "secondary", children: ["Secondary Button"] }}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
