import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import PaymentMethodItem from "../components/composites/PaymentMethodItem";

<Meta title="Molecules/PaymentMethodItem" component={PaymentMethodItem} />

# PaymentMethodItem

A simple card component used to represent payment methods. Must obligatorily receive the properties: `title`,
`description` and `icon`.

<style>{`
  .flex-container {
    margin: 0 auto;
    display: flex;
    justify-content: center;
    max-width: 270px;
  }
`}</style>

<Canvas>
    <Story
        name="Regular Payment Method Item"
        args={{
            title: "Cartão de Crédito",
            description: "Processamento instântaneo e seguro.",
            icon: "creditCard",
        }}
        argTypes={{ onClick: { action: "onClick" } }}
    >
        {args => <PaymentMethodItem {...args} />}
    </Story>
</Canvas>

<ArgsTable of={PaymentMethodItem} />
