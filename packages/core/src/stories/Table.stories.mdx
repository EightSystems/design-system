import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Layout/Table" />

Supports for React Virtual for large tables

Mimics web with `Table`, `TR`, `TD`, and also with the `data` prop.

