import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import SavedCardBadge from "../components/composites/SavedCardBadge";

<Meta title="Molecules/SavedCardBadge" component={SavedCardBadge} />

# SavedCardBadge

Component used to show a payment method previously registered by the user, and can be selected to allow the user to change the payment method that is going to be used.

<style>{`
  .flex-container {
    margin: 0 auto;
    display: flex;
    justify-content: center;
    max-width: 600px;
  }
`}</style>

<Canvas>
    <Story
        name="Regular SavedCardBadge"
        args={{ cardBrand: "maestro", cardLastDigits: "0132", cardIdentifier: "Cartão da Firma" }}
        argTypes={{ onClick: { action: "onClick" } }}
    >
        {args => (
            <div className="flex-container">
                <SavedCardBadge {...args} />
            </div>
        )}
    </Story>
</Canvas>

<ArgsTable of={SavedCardBadge} />
