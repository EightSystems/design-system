import styled from "styled-components";
import { textColors, fontSizes, fontFaces, fontWeights } from "../../../theme/mixins/web";

export const CurrencyLabelWrapper = styled.h1`
    ${fontFaces};
    ${fontSizes};
    ${textColors};
    ${fontWeights};
`;
