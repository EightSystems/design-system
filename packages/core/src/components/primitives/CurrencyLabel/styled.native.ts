import styled from "styled-components/native";
import { textColors, fontSizes, fontFaces } from "../../../theme/mixins/native";

export const CurrencyLabelWrapper = styled.Text`
    ${fontFaces};
    ${fontSizes};
    ${textColors};
`;
