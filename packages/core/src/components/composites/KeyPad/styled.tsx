import styled from "styled-components";
import Box from "../../layout/Box";
import { Row } from "../../layout/Grid";

export const KeyPadRow = styled(Row)`
    min-height: 85px;
`;

export const KeyPadContainer = styled(Box)`
    max-width: 360px;
`;
