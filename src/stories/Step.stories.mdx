import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Step from "../components/Step";

<Meta title="Atoms/Step" component={Step} />

# Step

> **Importante**: Esse componente faz parte de um componente composto e não foi projetado para ser usado de maneira
> independente.

Um componente que representa uma etapa em uma conjunto de etapas do componente `<Stepper>`. Deve receber
obrigatoriamente as propriedades `label`, e `active`.

<Canvas>
    <Story name="Step (Não-ativo)" args={{ label: "Confirmação de pagamento", active: false }}>
        {args => <Step {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Step (Ativo)" args={{ label: "Confirmação de pagamento", active: true }}>
        {args => <Step {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Step} />
