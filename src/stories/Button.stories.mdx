import { Meta, Story, Canvas, Description, ArgsTable } from "@storybook/addon-docs/blocks";
import Button from "../components/Button";

<Meta title="Atoms/Button" component={Button} />

<style>{`
  .flex-container {
    margin: 0 auto;
    display: flex;
    justify-content: center;
    margin: 36px;
  }
`}</style>

# Button

<Description>
    Os botões comunicam ações que o usuário pode realizar com um simples clique ou toque, podendo estar contidos em
    qualquer lugar da interface, desde caixas de diálogo e modais, até formulários e barras de navegação.
</Description>

<div className="flex-container">
    <Button color="primary">Botão Primário</Button>
</div>

## Variantes

Os botões possuem duas propriedades que definem a base de sua estilização: `color` e `outline`. Os dois podem ser usados
em conjunto para compor diferentes tipos de botão.

### Botão primário

<Canvas>
    <Story name="Primary Button" args={{color: 'primary', children: ['Botão Primário']}}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Primary Button (Outline)" args={{color: 'primary', hasOutline: true, children: ['Botão Primário']}}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

### Botão danger

<Canvas>
    <Story name="Danger Button" args={{color: 'danger', children: ['Botão Danger']}}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

<Canvas>
    <Story name="Danger Button (Outline)" args={{color: 'danger', hasOutline: true, children: ['Botão Danger']}}>
        {args => <Button {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
