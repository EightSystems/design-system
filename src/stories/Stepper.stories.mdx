import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Stepper from "../components/Stepper";

<Meta title="Molecules/Stepper" component={Stepper} />

# Stepper

O stepper exibe o progresso por meio de uma sequência de etapas lógicas lineares.
<Canvas>
    <Story name="Stepper" args={{ activeStepLabel: "Meio de pagamento", nextStepLabel: "Revisão", currentStepPercentage: 50, currentStep: 2, maxSteps: 4 }}>
        {args => <Stepper {...args} />}
    </Story>
</Canvas>

## Props

<ArgsTable of={Stepper} />
